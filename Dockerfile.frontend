# syntax=docker/dockerfile:1
<<<<<<< HEAD

FROM node:22-alpine as build
WORKDIR /app

# Instalacja zależności
COPY package*.json ./
RUN npm ci

# Kopiujemy kod
COPY . .

# Budujemy aplikację
RUN npm run build

# -------------------------------------
FROM nginx:alpine

# Kopiowanie zbudowanego frontendu
=======
FROM node:22-alpine as build
WORKDIR /app

COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build

FROM nginx:alpine

# Wrzucamy nasz własny nginx.conf
COPY ./nginx.conf /etc/nginx/conf.d/default.conf

# Kopiujemy build
>>>>>>> def9ccd (Poprawki)
COPY --from=build /app/dist /usr/share/nginx/html

EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
